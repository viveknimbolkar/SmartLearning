import { Text, Button } from "react-native";

export default function Home({ navigation }) {
  return (
    <>
      
      {/*
        Signup / Login buton Button 
      <Button
        title="Goto Welcome Screen"
        onPress={() => navigation.navigate("Welcome")}
      /> */}
      
    </>
  );
}
